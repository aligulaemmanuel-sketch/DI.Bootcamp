# Use a rotated PostgreSQL credential. Never commit .env.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/rysa_db?sslmode=require"

# Direct PostgreSQL URL used by Prisma Migrate. Required when DATABASE_URL uses Prisma Accelerate.
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/rysa_db?sslmode=require"
